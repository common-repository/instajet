;(function($){

    $.fn.ij = {};

    $.fn.ij.settings = {
        debug: true
    };

    $.fn.ij.init = function(options){

        var settings = $.fn.ij.settings;

        $.extend(settings, options);

        $.fn.ij.main();

    }

    $.fn.ij.main = function(){

        // main JS logic in here - unsure if correct pattern yet


    }







})(jQuery);